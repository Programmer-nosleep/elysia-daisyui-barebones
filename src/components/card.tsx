import { Html } from "@elysiajs/html";
import { PropsWithChildren } from "@kitajs/html";
import { Context } from "elysia";

export default function Card(context: Context) {
    return (
        <div class="">

        </div>
    );
}